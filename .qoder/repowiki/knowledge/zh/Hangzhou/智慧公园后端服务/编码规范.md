- 所有实体类继承com.park.common.entity.BaseEntity以自动管理id、createTime和updateTime字段。
- 控制器方法统一返回com.park.common.result.R<T>泛型对象，通过R.ok()或R.fail()构建标准化响应。
- 数据访问层接口继承MyBatis-Plus的BaseMapper，并放置在各自业务模块的mapper子包中。
- 业务逻辑层接口与实现分离，Service接口定义在service包，实现类通常位于service.impl或直接作为默认实现。