- 所有API函数均通过导入的 `request` 工具发起调用，并返回Promise对象以支持异步处理。
- 列表查询函数统一命名为 `get[Resource]List` 或 `get[Resource]Page`，并接受 `params` 对象作为分页和筛选参数。
- 新增与修改操作通常合并为一个 `save[Resource]` 函数，根据数据中是否包含 `id` 字段动态选择 `POST` 或 `PUT` 方法（见于 `system.js`）。
- 删除操作统一命名为 `delete[Resource]`，接收资源ID作为参数，并使用 `DELETE` 方法请求特定资源路径。