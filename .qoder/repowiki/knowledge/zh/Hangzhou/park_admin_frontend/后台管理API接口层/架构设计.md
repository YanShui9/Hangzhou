- 采用按业务域垂直拆分的模块化设计，每个文件（如 `audit.js`, `park.js`）对应一个独立的业务资源集合。
- 统一依赖 `@/utils/request` 作为HTTP客户端抽象层，实现请求拦截、响应处理及基础URL配置的集中管理。
- 遵循RESTful风格定义接口，通过命名函数（如 `getXXX`, `saveXXX`, `deleteXXX`）暴露CRUD操作，保持调用语义的一致性。
- 接口参数标准化：查询类使用 `params` 传递URL查询字符串，变更类使用 `data` 传递请求体，确保与后端接口的契约对齐。