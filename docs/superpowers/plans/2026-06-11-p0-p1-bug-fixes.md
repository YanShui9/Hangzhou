# P0/P1 Bug修复计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复park-platform项目的所有P0和P1问题，确保框架可靠运行

**Architecture:** 前后端分离架构，修复涉及前端API调用、后端安全性、数据权限控制等

**Tech Stack:** Vue2, ElementUI, SpringBoot 2.7.x, MyBatis-Plus, JWT

---

## 修复任务总览

| 任务 | 优先级 | 问题描述 | 影响范围 |
|------|--------|----------|----------|
| Task 1 | 🔴 P0 | 前端API路径重复 | 全部接口调用失败 |
| Task 2 | 🔴 P0 | 用户列表泄露密码哈希 | 安全漏洞 |
| Task 3 | 🔴 P0 | 评价模块缺少数据权限 | 数据隔离失效 |
| Task 4 | 🔴 P0 | 评价审核接口未暴露 | 核心功能不可用 |
| Task 5 | 🔴 P0 | BaseEntity缺少自动填充 | 数据一致性 |
| Task 6 | 🟡 P1 | 区县管理员多园区查询 | 功能缺陷 |
| Task 7 | 🟡 P1 | 响应拦截器返回层级统一 | 前端规范 |

---

### Task 1: 修复前端API路径重复问题

**Files:**
- Modify: `park-admin/src/utils/request.js`

**问题描述:**
`request.js` 的 `baseURL` 设置为 `/api`，而所有API文件中的URL又以 `/api/` 开头，导致最终请求路径变成 `/api/api/auth/login`，所有接口404。

- [ ] **Step 1: 修改request.js的baseURL**

```javascript
// park-admin/src/utils/request.js
const service = axios.create({
  baseURL: '/',  // 修改为 '/'，因为后端接口统一以 /api/ 开头
  timeout: 10000
})
```

- [ ] **Step 2: 验证API路径**

启动前端项目，打开浏览器开发者工具，确认请求路径为 `/api/auth/login` 而非 `/api/api/auth/login`。

- [ ] **Step 3: 提交代码**

```bash
git add park-admin/src/utils/request.js
git commit -m "fix: 修复前端API路径重复问题，baseURL改为'/'"
```

---

### Task 2: 修复用户列表接口泄露密码哈希

**Files:**
- Modify: `park-server/src/main/java/com/park/auth/entity/SysUser.java`
- Modify: `park-server/src/main/java/com/park/system/controller/UserController.java`

**问题描述:**
`getUserPage` 直接返回 `IPage<SysUser>`，密码哈希会被返回给前端。

- [ ] **Step 1: 在SysUser实体中排除password字段的默认查询**

```java
// park-server/src/main/java/com/park/auth/entity/SysUser.java
@Data
@EqualsAndHashCode(callSuper = true)
@TableName("sys_user")
@ApiModel(description = "系统用户")
public class SysUser extends BaseEntity {

    private static final long serialVersionUID = 1L;

    @ApiModelProperty(value = "用户名")
    private String username;

    @TableField(select = false)  // 默认查询不包含password字段
    @ApiModelProperty(value = "密码")
    private String password;

    @ApiModelProperty(value = "真实姓名")
    private String realName;

    @ApiModelProperty(value = "手机号")
    private String phone;

    @ApiModelProperty(value = "角色类型：1=市级管理员，2=区县管理员，3=园区管理员")
    private Integer roleType;

    @ApiModelProperty(value = "所属区县ID")
    private Long districtId;

    @ApiModelProperty(value = "所属园区ID")
    private Long parkId;

    @ApiModelProperty(value = "状态：0=禁用，1=启用")
    private Integer status;
}
```

- [ ] **Step 2: 修改UserService中的登录方法，显式查询password字段**

```java
// park-server/src/main/java/com/park/auth/service/AuthService.java
public LoginVO login(LoginDTO loginDTO) {
    // 显式查询包含password字段
    LambdaQueryWrapper<SysUser> wrapper = new LambdaQueryWrapper<>();
    wrapper.eq(SysUser::getUsername, loginDTO.getUsername())
           .select(SysUser.class, info -> !info.getColumn().equals("password") 
                   || info.getColumn().equals("password"));  // 包含password
    SysUser user = userMapper.selectOne(wrapper);
    
    // ... 其余逻辑不变
}
```

或者使用更简单的方式，在登录时单独查询password：

```java
// park-server/src/main/java/com/park/auth/service/AuthService.java
public LoginVO login(LoginDTO loginDTO) {
    // 查询用户（不包含password）
    LambdaQueryWrapper<SysUser> wrapper = new LambdaQueryWrapper<>();
    wrapper.eq(SysUser::getUsername, loginDTO.getUsername());
    SysUser user = userMapper.selectOne(wrapper);
    
    if (user == null) {
        throw new BusinessException(ResultCode.USERNAME_OR_PASSWORD_ERROR);
    }
    
    // 单独查询password
    SysUser userWithPassword = userMapper.selectById(user.getId());
    if (!passwordEncoder.matches(loginDTO.getPassword(), userWithPassword.getPassword())) {
        throw new BusinessException(ResultCode.USERNAME_OR_PASSWORD_ERROR);
    }
    
    // ... 其余逻辑不变
}
```

- [ ] **Step 3: 验证用户列表接口**

调用 `GET /api/users` 接口，确认返回的用户数据中不包含 `password` 字段。

- [ ] **Step 4: 提交代码**

```bash
git add park-server/src/main/java/com/park/auth/entity/SysUser.java
git add park-server/src/main/java/com/park/auth/service/AuthService.java
git commit -m "fix: 修复用户列表接口泄露密码哈希问题"
```

---

### Task 3: 为评价模块添加数据权限控制

**Files:**
- Modify: `park-server/src/main/java/com/park/evaluation/controller/EvaluationController.java`

**问题描述:**
`getEvaluationPage` 没有调用 `applyDataPermission`，任何用户都能查看所有评价记录。

- [ ] **Step 1: 添加数据权限控制方法**

```java
// park-server/src/main/java/com/park/evaluation/controller/EvaluationController.java
@Slf4j
@RestController
@RequestMapping("/api/evaluations")
@Api(tags = "评价管理")
public class EvaluationController {

    @Autowired
    private EvaluationService evaluationService;
    
    @Autowired
    private AuthService authService;
    
    @Autowired
    private ParkMapper parkMapper;

    /**
     * 分页查询评价记录列表
     */
    @GetMapping
    @ApiOperation(value = "分页查询评价记录", notes = "根据条件分页查询评价记录列表")
    public R<PageResult<EvaluationRecord>> getEvaluationPage(
            EvaluationQueryDTO queryDTO,
            HttpServletRequest request) {
        
        // 应用数据权限
        applyDataPermission(queryDTO, request);
        
        IPage<EvaluationRecord> page = evaluationService.getEvaluationPage(queryDTO);
        List<EvaluationRecord> records = page.getRecords();
        PageResult<EvaluationRecord> pageResult = PageResult.of(
                records,
                page.getTotal(),
                (int) page.getCurrent(),
                (int) page.getSize()
        );
        return R.ok(pageResult);
    }

    /**
     * 应用数据权限控制
     * 市级管理员：查看所有
     * 区县管理员：查看本区县的评价记录
     * 园区管理员：查看本园区的评价记录
     */
    private void applyDataPermission(EvaluationQueryDTO queryDTO, HttpServletRequest request) {
        Integer roleType = (Integer) request.getAttribute("roleType");
        Long userId = (Long) request.getAttribute("userId");

        if (roleType == null || roleType == 1) {
            // 市级管理员，查看所有
            return;
        }

        // 获取用户信息
        SysUser user = authService.getUserById(userId);
        if (user == null) {
            throw new BusinessException(ResultCode.USER_NOT_FOUND);
        }

        if (roleType == 2) {
            // 区县管理员：查看本区县的评价记录
            if (user.getDistrictId() == null) {
                throw new BusinessException(ResultCode.PERMISSION_DENIED, "区县管理员未分配区县");
            }
            // 查询该区县下的所有园区ID
            List<Long> parkIds = parkMapper.selectList(
                new LambdaQueryWrapper<ParkInfo>()
                    .eq(ParkInfo::getDistrictId, user.getDistrictId())
                    .select(ParkInfo::getId)
            ).stream().map(ParkInfo::getId).collect(Collectors.toList());
            
            if (parkIds.isEmpty()) {
                queryDTO.setParkId(-1L); // 无数据
            } else {
                queryDTO.setParkIds(parkIds);
            }
        } else if (roleType == 3) {
            // 园区管理员：查看本园区的评价记录
            if (user.getParkId() == null) {
                throw new BusinessException(ResultCode.PERMISSION_DENIED, "园区管理员未分配园区");
            }
            queryDTO.setParkId(user.getParkId());
        }
    }
    
    // ... 其余方法不变
}
```

- [ ] **Step 2: 修改EvaluationQueryDTO支持多园区查询**

```java
// park-server/src/main/java/com/park/evaluation/dto/EvaluationQueryDTO.java
@Data
@EqualsAndHashCode(callSuper = true)
@ApiModel(description = "评价查询条件")
public class EvaluationQueryDTO extends PageQuery {

    @ApiModelProperty(value = "园区ID")
    private Long parkId;

    @ApiModelProperty(value = "园区ID列表（区县管理员使用）")
    private List<Long> parkIds;

    @ApiModelProperty(value = "评价年度")
    private Integer year;

    @ApiModelProperty(value = "状态：0=草稿, 1=待区县审, 2=待市局审, 3=通过, 4=驳回")
    private Integer status;
}
```

- [ ] **Step 3: 修改EvaluationService支持多园区查询**

```java
// park-server/src/main/java/com/park/evaluation/service/EvaluationService.java
public IPage<EvaluationRecord> getEvaluationPage(EvaluationQueryDTO queryDTO) {
    Page<EvaluationRecord> page = new Page<>(queryDTO.getPageNum(), queryDTO.getPageSize());
    LambdaQueryWrapper<EvaluationRecord> wrapper = new LambdaQueryWrapper<>();

    // 构建查询条件
    if (queryDTO.getParkId() != null) {
        wrapper.eq(EvaluationRecord::getParkId, queryDTO.getParkId());
    }
    if (queryDTO.getParkIds() != null && !queryDTO.getParkIds().isEmpty()) {
        wrapper.in(EvaluationRecord::getParkId, queryDTO.getParkIds());
    }
    if (queryDTO.getYear() != null) {
        wrapper.eq(EvaluationRecord::getYear, queryDTO.getYear());
    }
    if (queryDTO.getStatus() != null) {
        wrapper.eq(EvaluationRecord::getStatus, queryDTO.getStatus());
    }

    // 默认按创建时间降序排序
    wrapper.orderByDesc(EvaluationRecord::getCreateTime);

    return evaluationMapper.selectPage(page, wrapper);
}
```

- [ ] **Step 4: 验证数据权限**

使用不同角色登录，验证：
- 市级管理员：能看到所有评价记录
- 区县管理员：只能看到本区县的评价记录
- 园区管理员：只能看到本园区的评价记录

- [ ] **Step 5: 提交代码**

```bash
git add park-server/src/main/java/com/park/evaluation/controller/EvaluationController.java
git add park-server/src/main/java/com/park/evaluation/dto/EvaluationQueryDTO.java
git add park-server/src/main/java/com/park/evaluation/service/EvaluationService.java
git commit -m "fix: 为评价模块添加数据权限控制"
```

---

### Task 4: 暴露评价审核接口

**Files:**
- Modify: `park-server/src/main/java/com/park/evaluation/controller/EvaluationController.java`

**问题描述:**
`EvaluationService` 中实现了 `districtPass/districtReject/cityPass/cityReject` 四个审核方法，但 `EvaluationController` 中没有对应的API端点。

- [ ] **Step 1: 添加审核接口**

```java
// park-server/src/main/java/com/park/evaluation/controller/EvaluationController.java

/**
 * 区县审核通过（状态从 1=待区县审 改为 2=待市局审）
 */
@PostMapping("/{id}/district-pass")
@ApiOperation(value = "区县审核通过", notes = "区县管理员审核通过评价记录")
public R<Void> districtPass(@PathVariable Long id, HttpServletRequest request) {
    checkAuditPermission(request, 2); // 校验区县管理员权限
    evaluationService.districtPass(id);
    return R.ok();
}

/**
 * 区县审核驳回（状态从 1=待区县审 改为 4=驳回）
 */
@PostMapping("/{id}/district-reject")
@ApiOperation(value = "区县审核驳回", notes = "区县管理员审核驳回评价记录")
public R<Void> districtReject(@PathVariable Long id, HttpServletRequest request) {
    checkAuditPermission(request, 2); // 校验区县管理员权限
    evaluationService.districtReject(id);
    return R.ok();
}

/**
 * 市级审核通过（状态从 2=待市局审 改为 3=通过）
 */
@PostMapping("/{id}/city-pass")
@ApiOperation(value = "市级审核通过", notes = "市级管理员审核通过评价记录")
public R<Void> cityPass(@PathVariable Long id, HttpServletRequest request) {
    checkAuditPermission(request, 1); // 校验市级管理员权限
    evaluationService.cityPass(id);
    return R.ok();
}

/**
 * 市级审核驳回（状态从 2=待市局审 改为 4=驳回）
 */
@PostMapping("/{id}/city-reject")
@ApiOperation(value = "市级审核驳回", notes = "市级管理员审核驳回评价记录")
public R<Void> cityReject(@PathVariable Long id, HttpServletRequest request) {
    checkAuditPermission(request, 1); // 校验市级管理员权限
    evaluationService.cityReject(id);
    return R.ok();
}

/**
 * 校验审核权限
 */
private void checkAuditPermission(HttpServletRequest request, Integer requiredRoleType) {
    Integer roleType = (Integer) request.getAttribute("roleType");
    if (roleType == null || !roleType.equals(requiredRoleType)) {
        throw new BusinessException(ResultCode.PERMISSION_DENIED, "无审核权限");
    }
}
```

- [ ] **Step 2: 验证审核接口**

使用不同角色测试：
- 区县管理员调用 `/api/evaluations/{id}/district-pass` 应成功
- 市级管理员调用 `/api/evaluations/{id}/city-pass` 应成功
- 园区管理员调用审核接口应返回403

- [ ] **Step 3: 提交代码**

```bash
git add park-server/src/main/java/com/park/evaluation/controller/EvaluationController.java
git commit -m "feat: 暴露评价审核接口，添加角色权限校验"
```

---

### Task 5: 为BaseEntity添加自动填充配置

**Files:**
- Modify: `park-server/src/main/java/com/park/common/entity/BaseEntity.java`
- Create: `park-server/src/main/java/com/park/config/MybatisPlusConfig.java` (如果不存在)

**问题描述:**
`createTime` 和 `updateTime` 没有使用 `@TableField(fill = FieldFill.INSERT)` 注解，MyBatis-Plus的 `updateById` 不会自动更新 `updateTime`。

- [ ] **Step 1: 修改BaseEntity添加自动填充注解**

```java
// park-server/src/main/java/com/park/common/entity/BaseEntity.java
@Data
public class BaseEntity implements Serializable {

    private static final long serialVersionUID = 1L;

    @TableId(value = "id", type = IdType.AUTO)
    @ApiModelProperty(value = "主键ID")
    private Long id;

    @TableField(fill = FieldFill.INSERT)
    @ApiModelProperty(value = "创建时间")
    private LocalDateTime createTime;

    @TableField(fill = FieldFill.INSERT_UPDATE)
    @ApiModelProperty(value = "更新时间")
    private LocalDateTime updateTime;
}
```

- [ ] **Step 2: 创建MetaObjectHandler配置**

```java
// park-server/src/main/java/com/park/config/MybatisPlusConfig.java
package com.park.config;

import com.baomidou.mybatisplus.core.handlers.MetaObjectHandler;
import lombok.extern.slf4j.Slf4j;
import org.apache.ibatis.reflection.MetaObject;
import org.springframework.stereotype.Component;

import java.time.LocalDateTime;

/**
 * MyBatis-Plus自动填充处理器
 */
@Slf4j
@Component
public class AutoFillMetaObjectHandler implements MetaObjectHandler {

    @Override
    public void insertFill(MetaObject metaObject) {
        log.info("自动填充创建时间");
        this.strictInsertFill(metaObject, "createTime", LocalDateTime.class, LocalDateTime.now());
        this.strictInsertFill(metaObject, "updateTime", LocalDateTime.class, LocalDateTime.now());
    }

    @Override
    public void updateFill(MetaObject metaObject) {
        log.info("自动填充更新时间");
        this.strictUpdateFill(metaObject, "updateTime", LocalDateTime.class, LocalDateTime.now());
    }
}
```

- [ ] **Step 3: 验证自动填充**

新增一条记录，检查 `create_time` 和 `update_time` 是否自动填充。修改记录，检查 `update_time` 是否自动更新。

- [ ] **Step 4: 提交代码**

```bash
git add park-server/src/main/java/com/park/common/entity/BaseEntity.java
git add park-server/src/main/java/com/park/config/AutoFillMetaObjectHandler.java
git commit -m "feat: 为BaseEntity添加自动填充配置"
```

---

### Task 6: 修复区县管理员企业查询的多园区场景

**Files:**
- Modify: `park-server/src/main/java/com/park/enterprise/controller/EnterpriseController.java`
- Modify: `park-server/src/main/java/com/park/enterprise/dto/EnterpriseQueryDTO.java`
- Modify: `park-server/src/main/java/com/park/enterprise/service/EnterpriseService.java`

**问题描述:**
当区县管理员所属区县下有多个园区时，查询逻辑需要使用 `IN` 而非 `=`。

- [ ] **Step 1: 修改EnterpriseQueryDTO支持多园区查询**

```java
// park-server/src/main/java/com/park/enterprise/dto/EnterpriseQueryDTO.java
@Data
@EqualsAndHashCode(callSuper = true)
@ApiModel(description = "企业查询条件")
public class EnterpriseQueryDTO extends PageQuery {

    @ApiModelProperty(value = "企业名称")
    private String enterpriseName;

    @ApiModelProperty(value = "所属园区ID")
    private Long parkId;

    @ApiModelProperty(value = "园区ID列表（区县管理员使用）")
    private List<Long> parkIds;

    @ApiModelProperty(value = "行业门类代码")
    private String industryCode;

    @ApiModelProperty(value = "经营状态")
    private String status;

    @ApiModelProperty(value = "是否参评：0=不参评，1=参评")
    private Integer isParticipate;
}
```

- [ ] **Step 2: 修改EnterpriseService支持多园区查询**

```java
// park-server/src/main/java/com/park/enterprise/service/EnterpriseService.java
public IPage<EnterpriseInfo> getEnterprisePage(EnterpriseQueryDTO queryDTO) {
    Page<EnterpriseInfo> page = new Page<>(queryDTO.getPageNum(), queryDTO.getPageSize());
    LambdaQueryWrapper<EnterpriseInfo> wrapper = new LambdaQueryWrapper<>();

    // 构建查询条件
    if (StringUtils.isNotBlank(queryDTO.getEnterpriseName())) {
        wrapper.like(EnterpriseInfo::getEnterpriseName, queryDTO.getEnterpriseName());
    }
    if (queryDTO.getParkId() != null) {
        wrapper.eq(EnterpriseInfo::getParkId, queryDTO.getParkId());
    }
    if (queryDTO.getParkIds() != null && !queryDTO.getParkIds().isEmpty()) {
        wrapper.in(EnterpriseInfo::getParkId, queryDTO.getParkIds());
    }
    if (StringUtils.isNotBlank(queryDTO.getIndustryCode())) {
        wrapper.eq(EnterpriseInfo::getIndustryCode, queryDTO.getIndustryCode());
    }
    if (StringUtils.isNotBlank(queryDTO.getStatus())) {
        wrapper.eq(EnterpriseInfo::getStatus, queryDTO.getStatus());
    }
    if (queryDTO.getIsParticipate() != null) {
        wrapper.eq(EnterpriseInfo::getIsParticipate, queryDTO.getIsParticipate());
    }

    // 默认按创建时间降序排序
    wrapper.orderByDesc(EnterpriseInfo::getCreateTime);

    return enterpriseMapper.selectPage(page, wrapper);
}
```

- [ ] **Step 3: 修改EnterpriseController的数据权限控制**

```java
// park-server/src/main/java/com/park/enterprise/controller/EnterpriseController.java
private void applyDataPermission(EnterpriseQueryDTO queryDTO, HttpServletRequest request) {
    Integer roleType = (Integer) request.getAttribute("roleType");
    Long userId = (Long) request.getAttribute("userId");

    if (roleType == null || roleType == 1) {
        // 市级管理员，查看所有
        return;
    }

    // 获取用户信息
    SysUser user = authService.getUserById(userId);
    if (user == null) {
        throw new BusinessException(ResultCode.USER_NOT_FOUND);
    }

    if (roleType == 2) {
        // 区县管理员：查看本区县的企业
        if (user.getDistrictId() == null) {
            throw new BusinessException(ResultCode.PERMISSION_DENIED, "区县管理员未分配区县");
        }
        // 查询该区县下的所有园区ID
        List<Long> parkIds = parkMapper.selectList(
            new LambdaQueryWrapper<ParkInfo>()
                .eq(ParkInfo::getDistrictId, user.getDistrictId())
                .select(ParkInfo::getId)
        ).stream().map(ParkInfo::getId).collect(Collectors.toList());
        
        if (parkIds.isEmpty()) {
            queryDTO.setParkId(-1L); // 无数据
        } else {
            queryDTO.setParkIds(parkIds);
        }
    } else if (roleType == 3) {
        // 园区管理员：查看本园区的企业
        if (user.getParkId() == null) {
            throw new BusinessException(ResultCode.PERMISSION_DENIED, "园区管理员未分配园区");
        }
        queryDTO.setParkId(user.getParkId());
    }
}
```

- [ ] **Step 4: 验证多园区查询**

创建区县管理员（如西湖区），该区县下有多个园区，验证能看到所有园区的企业。

- [ ] **Step 5: 提交代码**

```bash
git add park-server/src/main/java/com/park/enterprise/controller/EnterpriseController.java
git add park-server/src/main/java/com/park/enterprise/dto/EnterpriseQueryDTO.java
git add park-server/src/main/java/com/park/enterprise/service/EnterpriseService.java
git commit -m "fix: 修复区县管理员企业查询的多园区场景"
```

---

### Task 7: 统一前端响应拦截器返回层级

**Files:**
- Modify: `park-admin/src/utils/request.js`

**问题描述:**
响应拦截器返回整个 `R` 对象，前端代码需要统一按 `response.data` 解构。

- [ ] **Step 1: 修改响应拦截器返回R.data**

```javascript
// park-admin/src/utils/request.js
service.interceptors.response.use(
  response => {
    const res = response.data
    
    // 如果后端返回的code不是200，说明有错误
    if (res.code && res.code !== 200) {
      Message({
        message: res.message || '请求失败',
        type: 'error',
        duration: 2000
      })
      
      // 401: Token过期或未登录
      if (res.code === 401) {
        MessageBox.confirm(
          '登录状态已过期，请重新登录',
          '系统提示',
          {
            confirmButtonText: '重新登录',
            cancelButtonText: '取消',
            type: 'warning'
          }
        ).then(() => {
          store.dispatch('user/logout').then(() => {
            location.reload()
          })
        })
      }
      
      return Promise.reject(new Error(res.message || '请求失败'))
    }
    
    // 返回R对象，让调用方通过 response.data 获取数据
    return res
  },
  error => {
    console.log('err' + error)
    Message({
      message: error.message || '网络错误',
      type: 'error',
      duration: 2000
    })
    return Promise.reject(error)
  }
)
```

- [ ] **Step 2: 统一所有页面的API调用方式**

确保所有页面都按 `response.data` 解构：

```javascript
// 示例：登录页面
login(loginForm).then(response => {
  const { data } = response  // response是R对象，data是LoginVO
  // ...
})

// 示例：列表页面
getList().then(response => {
  const { records, total } = response.data  // response是R对象，response.data是分页数据
  // ...
})
```

- [ ] **Step 3: 验证API调用**

测试登录、列表查询等功能，确认数据能正确获取。

- [ ] **Step 4: 提交代码**

```bash
git add park-admin/src/utils/request.js
git commit -m "fix: 统一前端响应拦截器返回层级"
```

---

## 验证清单

完成所有修复后，按以下清单验证：

| 验证项 | 验证方法 | 预期结果 |
|--------|----------|----------|
| API路径 | 浏览器开发者工具 | 请求路径为 `/api/auth/login` |
| 密码泄露 | 调用 `GET /api/users` | 返回数据不含password字段 |
| 评价权限 | 区县管理员登录查看评价 | 只能看到本区县的评价 |
| 审核接口 | 调用审核接口 | 角色权限正确校验 |
| 时间填充 | 新增/修改记录 | createTime和updateTime自动填充 |
| 多园区查询 | 区县管理员查看企业 | 能看到所有园区的企业 |
| 响应层级 | 所有API调用 | 数据正确获取 |

---

## 执行建议

1. **按顺序执行**：Task 1-5 是 P0 问题，必须先修复
2. **每完成一个Task就验证**：确保修复有效
3. **Task 2 和 Task 5 可能需要重启后端**：因为涉及实体类修改
4. **Task 7 需要检查所有前端页面**：确保API调用方式统一

---

**计划完成，是否开始执行修复？**
