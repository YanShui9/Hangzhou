# README 更新与默认分支同步设计

## 目标

将 `newlife` 分支截至 `314869f` 的项目版本同步到默认分支 `main`，并更新仓库首页 README，使其准确反映当前课程团队项目的功能、代码版本和运行方式。

## 分支策略

- `origin/main` 是 `314869f` 的祖先，使用快进方式同步，不改写远端历史。
- README 更新先在 `newlife` 上形成独立提交，再将同一提交快进到 `main`。
- 不提交当前工作区中与本任务无关的 `JwtAuthenticationFilter.java` 修改、答辩材料或其他未跟踪文件。

## README 内容结构

1. 项目定位：明确为课程团队项目和非生产系统。
2. 项目亮点：三级角色数据范围、评价填报与审核、结果分档、数据仓库和可视化。
3. 核心业务流程：按当前本地版本描述 `0 草稿 → 1 待区县审核 → 2 区县通过 → 5 已上报 → 3 通过 / 4 驳回`。
4. 技术架构：保留 Spring Boot、MyBatis-Plus、Vue 2、MySQL、JWT、ECharts 等可由代码确认的内容。
5. 功能与目录：用模块级概览替代逐表、逐文件的大段枚举。
6. 快速开始：给出数据库、后端、前端的最短可复现步骤和配置安全提示。
7. 项目说明：不把需求指标写成实测数据，不声称已部署到政府生产环境。

## 验证

- 检查 README 中审核状态与当前代码一致。
- 检查 README 引用的路径在仓库中存在。
- 检查 Git 暂存区只包含设计记录和 README。
- 提交后验证 `newlife` 与 `main` 指向同一新提交，且远端默认分支可读取新版 README。

